/* ============================================================
 * Live HTML — 사이트 설정 (딱 한 줄만 채우면 끝)
 *
 * 아래 GAS_URL 에 Apps Script 웹 앱 주소(…/exec)를 넣어두면,
 * 어떤 기기에서든 관리자 모드에 들어가 "비밀번호만" 입력하면
 * 내 디자인 전체를 바로 볼 수 있어요. (주소·아이디 재입력 불필요)
 *
 * ────────────────────────────────────────────────────────────
 * 🔒 보안 안내 — 꼭 읽어주세요
 *  - 이 '주소(URL)'는 공개돼도 안전합니다. 진짜 잠금은 '비밀번호(SECRET)'예요.
 *  - 비밀번호는 절대 이 파일이나 코드/저장소에 넣지 마세요.
 *    (앱 실행 중 관리자 입장 시 한 번만 입력 → 이 브라우저에만 기억됨)
 *  - 비밀번호는 Apps Script의 [프로젝트 설정 ⚙️ > 스크립트 속성 > SECRET]에만
 *    저장되며 서버에서 검증됩니다. 주소를 알아도 비밀번호가 없으면 아무것도 못 봐요.
 * ────────────────────────────────────────────────────────────
 *
 * 채우는 법:
 *  1) Apps Script 배포 후 발급된 '…/exec' 주소를 아래 따옴표 안에 붙여넣기
 *  2) 저장하고 커밋(push) → 끝. 모든 기기에서 비밀번호만으로 접속됩니다.
 *
 *  예) GAS_URL: "https://script.google.com/macros/s/AKfycb.../exec"
 * ============================================================ */
window.LIVEHTML_CONFIG = {
  // 여기에 Apps Script 웹 앱 주소를 붙여넣으세요 (비워 두면 기존처럼 기기마다 주소 입력)
  GAS_URL: "",
};
